#include<stdio.h>
int factorial(int);
int main()
{
	int n,fact;
	printf("enter the element=");
	scanf("%d",&n);
	fact=factorial(n);
	printf("fact of %d = %d",n,fact);
}
int factorial(int n)
{
	if(n==0)
	return 1;
	else
	return(n*factorial(n-1));

}
